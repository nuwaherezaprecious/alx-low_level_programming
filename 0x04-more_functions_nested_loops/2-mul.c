#include "main.h"

/**
 * mul - multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply
 * Return: result of multiplication of a param and b param
 */
int mul(int a, int b)
{
	return (a * b);
}
